<div class="parentheader">
	<h1>全部用户</h1>
	<%= tabs_tag(:namespace => :navigation, :builder => ApplicationHelper::MenuTabBuilder, :open_tabs => { :class => "tabs" }) do |tab| %>
		<%= tab.rp 		'RP',		users_rp_url %>
		<%= tab.oldest 	'时间',	users_oldest_url%>
	<% end %>
</div>
<%= render :partial => 'partials/user_card', :collection => @users, :as => :user %>
<%= will_paginate @users %>